
#include <sys/types.h>
#include <unistd.h>

//    volatile int i = 0;
//    static char hostname[256] = "local johnlu";
//    printf("PID %d on %s ready for attach\n", getpid(), hostname);
//    fflush(stdout);
//    while (0 == i)
//      sleep(5);